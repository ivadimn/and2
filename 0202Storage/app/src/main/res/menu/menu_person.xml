<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:id="@+id/group_save">
        <item android:id="@+id/menuitem_save_id"
            android:title="@string/menuitem_save"
            app:showAsAction="always"
            />
        <item android:id="@+id/menuitem_add_id"
            android:title="@string/menuitem_addfriend"
            app:showAsAction="ifRoom"
            />
    </group>
    <item android:id="@+id/menuitem_deletefriend_id"
        android:title="@string/menuitem_delfriend"
        app:showAsAction="always"
    />

</menu>